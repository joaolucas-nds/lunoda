# Log — 2026-07-19 (sessão 3)

> Sessão 1 (template-update do KCM) e sessão 2 (FIX-002 + testes) do mesmo dia estão nos logs anteriores.

## Objetivo da sessão
Verificar se a v12.1 saiu como planejado e prosseguir com o desenvolvimento.

## Feito
- **Verificação da v12.1 aplicada** — não por confiança, por medição, contra o `index.html` real do repositório:
  - smoke **13/13** (o usuário havia reproduzido o mesmo: 8 ok/5 falhas antes, 13/0 depois);
  - `node --check` no JS extraído: sintaxe válida;
  - **sem duplicação de âncora** (regressão clássica de patch): `entryToMD`, `exportAllJSON` e `const DIV` aparecem 1× cada;
  - `saveDB` de uma linha **removido**; `requestPersistence` e `warnIfStorageTight` presentes 1× cada;
  - arquivo 2.771 → 2.842 linhas.
- **Desenvolvida a v12.2 — undo/redo estrutural** (F8 parte 2), entregue como `260719-asu0002.yaml` com 3 modificações: motor de histórico, botões no cabeçalho, atalhos de teclado.
- **Criado `tests/history.mjs`** — 18 asserções sobre o motor, com editor de mentira (dublês de DOM e mutadores).
- Validação em cópia: **história 18/18**, **smoke 13/13** (sem regressão), sintaxe OK, HTML dos botões conferido.

## Decisões
- **DEC-010** — undo por snapshot inteiro do editor (reaproveitando `getEditorSnapshot()`), com os 11 mutadores **embrulhados** por lista de nomes em vez de editados um a um. Escopo: só operações estruturais; digitação fica com o desfazer nativo do navegador.

## Bugs
- Nenhum bug de código nesta sessão. Dois erros meus, pegos antes da entrega:
  1. Escrevi `redoEditorNoop_undo()` no lugar de `undoEditor()` no rascunho da instrução — corrigido antes do primeiro teste.
  2. Os botões multi-linha saíram com indentação irregular ao serem inseridos — reescritos numa linha só, seguindo o estilo dos botões vizinhos.

## Aprendizados / armadilhas
- **O teste falhou e o código estava certo.** A asserção "mutação nova limpa o redo" acusou falha porque meu `moveBlock` de mentira invertia uma lista de **um** elemento — inverter não mudava nada e o embrulho, corretamente, não registrava histórico. Quase se mexeu no que estava certo. Regra: investigar antes de "consertar"; fixture com um único elemento esconde bug de ordenação e finge bug onde não há.
- **Embrulhar 11 funções saiu melhor que editar 11 funções.** Diff minúsculo, regra num ponto só, e a próxima função nova entra acrescentando um nome à lista. Custo: indireção — mitigado por comentário e por teste.
- **Verificar patch aplicado tem passos além de "roda?"**: conferir que a âncora não duplicou e que o código antigo saiu. Sintaxe válida e teste verde não pegariam um bloco duplicado.

## Onde parei
v12.1 confirmada no repositório. v12.2 pronta e validada em cópia, **ainda não aplicada**.

## Próximos passos
1. Aplicar: `python -m src apply 260719-asu0002.yaml --root C:\Users\alexk\Tools\Lunoda\lunoda --dry-run` e depois sem `--dry-run`.
2. `node tests/history.mjs` (18 ok) e `node tests/smoke.mjs` (13 ok).
3. Testar no navegador: excluir blocos em massa e desfazer; `Ctrl+Z` dentro de um campo de texto deve desfazer a **digitação**, não a estrutura.
4. **Reexportar os `.md` antigos** — seguem com o defeito do FIX-002.
5. Escolher: **F9** (frontmatter YAML — pré-requisito já entregue) ou **F4** (UX de propriedades, exige resolver C1/C2/Q1 antes).
