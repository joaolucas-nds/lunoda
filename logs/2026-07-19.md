# Log — 2026-07-19

## Objetivo da sessão
Receber o template-update do KCM (Kit v1.74.0), comparar com o vivo, aplicar o que foi aprovado, migrar a documentação para `meta/`, pesquisar funcionalidades futuras e projetar as próximas fases.

## Feito
- Relido o `_UPDATE-MANIFEST.md`: destino real é **`meta/`** (eu havia suposto raiz na primeira leitura — erro corrigido).
- **CEREBRO (fusão):** adotada a base genérica do template (mais atual/refinada) + nova seção «Específico deste projeto (Lunoda)». Ganhos herdados: seções «Ao receber um template-update do KCM» e «Refino das Instruções do Projeto»; princípio 10 (pedido composto) e princípio 11 (concordância em busca-e-troca). ASU item 3 especializado para os nomes reais dos docs.
- **Docs migrados para `meta/`** com conteúdo vivo preservado (cópia fiel + deltas, sem reescrever de memória).
- **CONTEXT:** + «O que o Lunoda deliberadamente NÃO é» e + «Estrutura do projeto».
- **STATUS:** + «📁 Arquivos críticos (não mexer sem contexto)».
- **CHANGELOG:** + seção `[Não lançado]`.
- **DECISIONS:** + DEC-006 (docs em `meta/`) e DEC-007 (CEREBRO base do kit + seção do projeto).
- **INSTRUÇÕES:** especializadas para o Lunoda — `[NOME]` substituído, armadilhas do código promovidas para leitura em todo turno, caminhos `meta/`. **5.405 caracteres** (antes 6.892 genéricos; teto do kit ~6.900).
- **`.flatdropignore`** adotado; **`.gitignore`** mesclado com a nota do template.
- **README** atualizado para a estrutura `meta/` + `guides/` + `logs/`.
- **Pesquisa** sobre teto do `localStorage` e padrão de metadados em Markdown → 5 ideias novas no IDEAS e 4 fases novas no ROADMAP (F8–F11).

## Decisões
- **DEC-006** — docs de contexto em `meta/`; README do produto na raiz; guias em `guides/`.
- **DEC-007** — CEREBRO adota a base do kit + seção específica do projeto (o CEREBRO vivo não tinha customização a preservar).
- Entrega em **arquivos completos**, não ASU: a maior parte é reescrita profunda e docs rolantes, casos em que o próprio CEREBRO manda entregar inteiro.

## Bugs
- Nenhum bug de código. Nenhuma alteração no `Lunoda_v12.html`.

## Aprendizados / armadilhas
- **Erro meu:** afirmei "Kit v1.73.0" no primeiro relatório; a verificação direta mostrou **v1.74.0**. E supus os docs na raiz sem reler o manifesto. Lição: conferir no arquivo, não no que a leitura anterior pareceu dizer — sobretudo versão e caminho.
- **O mount perdeu os arquivos vivos** entre as duas sessões (sobraram só os `__template-update`). Os docs sobreviveram porque eu os havia gerado e eles seguiram em `outputs/`. Reforça o valor de guias e docs versionados no Git.
- **Pesquisa mudou a prioridade do roadmap:** o teto de ~5 MB do `localStorage` e a ausência de undo transformaram a "rede de segurança" (F8) na fase de maior retorno — à frente da F4, que era a próxima.

## Onde parei
Atualização aplicada e documentação migrada para `meta/`. `Lunoda_v12.html` intocado. Escopo da v13 ainda em aberto (conflitos C1–C3 e Q1 seguem sem decisão).

## Próximos passos
- Decidir se a v13 começa pela **F8 (rede de segurança)** ou pela **F4 (UX de propriedades)** — minha recomendação é F8.
- Resolver C1, C2 e Q1 do IDEAS antes de projetar a F4.
- Decidir o modo de trabalho para o código: ASU (patch) ou Claude Code.
